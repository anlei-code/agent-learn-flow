# 大模型应用开发学习进度记录

最后更新时间：2026-06-18

## 当前状态

- 当前阶段：第二课，LLM Client 封装。
- 当前项目：[knowledgeflow-copilot](knowledgeflow-copilot/README.md)
- 当前课程：[第 2 课：封装 LLM Client](knowledgeflow-copilot/lessons/02_llm_client.md)

## 第一阶段目标

本阶段先不接真实大模型 API，重点是把后端工程骨架搭起来：

1. 会启动一个 FastAPI 服务。
2. 会用 Pydantic 定义请求和响应。
3. 会用环境变量管理配置。
4. 会写基础测试。
5. 会读懂一个可扩展项目结构。

## 已完成

- 新建 `knowledgeflow-copilot` 项目目录。
- 新增 FastAPI 应用入口：`app/main.py`。
- 新增配置模块：`app/core/config.py`。
- 新增日志模块：`app/core/logging.py`。
- 新增 Pydantic 模型：`app/schemas.py`。
- 新增 API 路由：`app/api/routes.py`。
- 新增测试：`tests/test_api.py`。
- 新增第一课学习说明：`lessons/01_fastapi_pydantic.md`。
- 已创建本地虚拟环境：`knowledgeflow-copilot/.venv`。
- 已复制本地配置文件：`knowledgeflow-copilot/.env`。

## 本次验证结果

- 使用本机 `py -3.10` 创建虚拟环境并安装依赖成功。
- `pytest` 结果：4 个测试全部通过。
- `ruff check .` 结果：通过。
- 安装依赖时发现并修复了一个打包配置问题：`setuptools` 会把 `lessons` 误识别为顶层包，因此已在 `pyproject.toml` 中明确只打包 `app*`。
- 开发服务已启动并通过健康检查：`http://127.0.0.1:8001`。
- API 文档地址：`http://127.0.0.1:8001/docs`。
- 2026-06-18：已安装 `uv 0.11.21` 到当前用户目录：`C:\Users\Admin（无密码）\AppData\Roaming\Python\Python310\Scripts\uv.exe`。
- 已将 `uv` 所在目录加入用户 PATH；如果旧 PowerShell 仍提示无法识别，需要重新打开一个 PowerShell。
- 已执行 `uv sync --extra dev`，并生成 `knowledgeflow-copilot/uv.lock`。
- 已使用 `uv run pytest` 验证：4 个测试全部通过。
- 已使用 `uv run ruff check .` 验证：通过。
- 2026-06-18：处理了一次 `.venv\Lib` 删除失败问题。原因是项目 uvicorn/Python 进程仍在占用虚拟环境，停止进程后用 `uv venv --clear` 重建。
- 同次处理了 Windows 临时目录权限导致的 `Failed to update Windows PE resources`，通过设置 `TEMP/TMP/UV_CACHE_DIR` 到纯英文目录并使用 `uv sync --extra dev --link-mode=copy` 修复。

## 当前接口

- `GET /api/v1/health`：健康检查。
- `POST /api/v1/echo`：练习请求校验和响应模型，已返回 `reversed_message`。
- `POST /api/v1/chat/mock`：模拟聊天接口，后续会替换成真实 LLM 调用；当前 `message` 至少 2 个字符。
- `GET /api/v1/llm/status`：查看当前 LLM provider、模型和 API Key 配置状态。
- `POST /api/v1/chat`：统一聊天入口；没有 API Key 时走 mock，有 API Key 时可调用 OpenAI Responses API。
- `GET /api/v1/study/status`：返回当前阶段、阶段目标和下一步建议。

## 第一阶段练习完成记录

- 练习 1 已完成：`EchoResponse` 新增 `reversed_message`，并补充测试断言。
- 练习 2 已完成：`ChatRequest.message` 的最小长度改为 2，输入 1 个字符时返回 422。
- 练习 3 已完成：新增 `GET /api/v1/study/status` 接口。
- 验证结果：`uv run pytest` 6 个测试全部通过。
- 验证结果：`uv run ruff check .` 通过。

## 下一步

1. 打开 `http://127.0.0.1:8000/docs`，手动调用 `/api/v1/llm/status` 和 `/api/v1/chat`。
2. 读懂 `app/llm/client.py` 中 `LLMClient.chat()` 的 provider 切换逻辑。
3. 理解 `.env`、`Settings`、`LLMClient`、`routes.py` 的调用链。
4. 准备进入下一步：把真实 LLM 调用参数、错误处理和测试做得更完整。

## 下次继续时从这里开始

下次从“第二课复盘 + 真实 LLM 参数完善”继续。

## 第二课完成记录

- 新增 `app/llm/client.py`，封装 mock/OpenAI 两种 provider。
- 新增配置：`LLM_PROVIDER`、`OPENAI_API_KEY`、`OPENAI_MODEL`、`LLM_TIMEOUT_SECONDS`。
- 新增接口：`GET /api/v1/llm/status`。
- 新增接口：`POST /api/v1/chat`。
- 新增 OpenAI SDK 依赖，并通过 `uv.lock` 锁定。
- 测试中强制使用 mock provider，避免单元测试真实调用外部 API。
- 验证结果：`uv run pytest` 8 个测试全部通过。
- 验证结果：`uv run ruff check .` 通过。

## 第二课练习完成记录

- 练习 1 已完成：`/api/v1/chat` 已把 `temperature` 传入 `LLMClient.chat()`，mock 回复中会显示 temperature。
- 练习 2 已完成：新增 `OPENAI_SYSTEM_PROMPT`，并在 `_openai_chat()` 中替代写死的 `instructions`。
- 练习 3 已完成：补充 `LLM_PROVIDER=openai` 且没有 API Key 时返回 503 的测试。
- 验证结果：`uv run pytest` 9 个测试全部通过。
- 验证结果：`uv run ruff check .` 通过。
